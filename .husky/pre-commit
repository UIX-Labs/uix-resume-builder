echo ""
echo "🎨 Starting Format Check..."
pnpm run format
if [ $? -ne 0 ]; then
  echo "❌ Format failed. Fix formatting issues."
  exit 1
fi
echo "✅ Format successful!"


echo "🔍 Starting Lint Check..."
pnpm run lint
if [ $? -ne 0 ]; then
  echo "❌ Lint failed. Fix errors before committing."
  exit 1
fi
echo "✅ Lint successful!"

echo "🔍 Starting Test Check..."
pnpm run test
if [ $? -ne 0 ]; then
  echo "❌ Test failed. Fix test errors before committing."
  exit 1
fi
echo "✅ Test successful!"

echo "🔍 Starting Build Check..."
pnpm run build
if [ $? -ne 0 ]; then
  echo "❌ Build failed. Fix build errors before committing."
  exit 1
fi
echo "✅ Build successful!"

echo ""
echo "🎉 Pre-commit checks completed successfully!"